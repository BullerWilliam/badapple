services:
  - type: web
    name: badapple-api
    env: node
    plan: free
    buildCommand: pip install -r requirements.txt && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
